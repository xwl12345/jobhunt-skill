---
name: jobhunt
description: 通用秋招/校招求职执行助手：管理一个版本化的求职工作区（默认用 git，未安装且用户选择不装时降级为手动备份）。首次调用时自动进入计划阶段，收集学历、技能、实习/项目经历、意向职位等信息，分析并给出可行性高的求职方向规划（保底/主攻/冲刺三层）；此后长期维护求职进度打卡、投递跟踪、面试复盘找薄弱点、简历构建与更新，信息更新默认通过 git 提交留痕。当用户提到"秋招"、"校招"、"春招"、"求职"、"找工作"、"投递"、"笔试"、"面试复盘"、"改简历/做简历/生成简历"、"求职规划"，或想在新目录建立求职档案时使用——即使用户没有明说"秋招"两个字也要触发。
---

# 秋招执行助手（jobhunt）

帮一个求职者（通常是应届生）从零建立并长期维护一个**版本化的求职工作区**：默认用 git 管理版本；机器上没有 git 且用户选择不安装时，降级为带日期的手动备份，全部流程不变。工作区核心文件：

| 文件 | 作用 |
|---|---|
| `AGENTS.md` | 工作区接手指南（概况 / 文件地图 / 硬规矩），换电脑换 AI 工具都能接手 |
| `个人档案.md` | §0 统一口径与数据红线、§1 教育与技能盘点、§2 经历素材（STAR）、§3 待补强 |
| `投递跟踪.md` | 简历版本记录 + 投递记录表 + 保底/主攻/冲刺目标公司清单 |
| `进度与薄弱点.md` | 分模块学习进度 + 任务清单 + 面试复盘索引 |
| `简历/` | 姓名-岗位-年份 命名的 .md（内容源）/ .html（排版源）/ .pdf（成品） |

本 skill 的安装目录下 `assets/` 存有上述文件的模板（占位符版），`references/` 存放详细指引，**用到时再读**。本 skill 遵循开放 Agent Skills 标准（SKILL.md），不绑定任何特定 agent 平台；宿主专有能力一律按"有则用、无则降级"处理，不点名依赖任何专有工具。

## 运行环境与能力降级（开工前先读）

| 能力 | 级别 | 不可用时的处理 |
|---|---|---|
| 读写本地文件、执行 shell 命令 | 必需 | 不具备则无法运行本 skill，如实告知，不要假装完成 |
| git | 强烈建议 | 先 `git --version` 探测；缺失时按下文「git 缺失处理」走 |
| 联网搜索 | 建议 | 无则基于收集到的信息分析，规划中明确标注"未做当年行情核验" |
| 计划模式 / 结构化提问等宿主专有交互工具 | 可选 | 没有就用自然语言分批提问 + 文字方案确认，不依赖任何专有工具名 |
| 无头浏览器（Edge / Chrome / Chromium 任一） | 建议 | 都没有则引导用户用浏览器"打印 → 另存为 PDF"兜底 |

> Shell 语法说明：本 skill 的命令示例按 POSIX shell 书写（`&&` 连接、正斜杠路径），在 Git Bash / zsh / PowerShell 7+ 可直接运行；**Windows PowerShell 5.1 不支持 `&&`，把链式命令拆成两条依次执行即可**（如先 `git add -A`，再 `git commit -m "..."`）。

**git 缺失处理**（流程 0 初始化、后续每次提交前都可能遇到）：

1. 先讲清 git 的作用：每次改动都有可追溯历史、误删可回滚、换电脑或换 AI 助手时 clone 即接手。
2. **征求用户是否现在安装。用户同意 → 协助安装**（系统级操作，执行前说明可能需要管理员权限或弹出安装器，命令需用户确认后执行）：
   - Windows：优先 `winget install --id Git.Git -e --source winget`；没有 winget 则引导从 git-scm.com 下载安装器，安装完成后重开终端；
   - macOS：`xcode-select --install`（随命令行工具自带 git）或 `brew install git`；
   - Linux（Debian/Ubuntu）：`sudo apt update && sudo apt install -y git`；Fedora 系：`sudo dnf install -y git`。
   - 装完用 `git --version` 验证；验证失败如实报告，转入下一步。
3. **用户不同意、或安装失败 → 降级运行**：照常建立和维护工作区，但必须明确说明代价——没有提交历史、无法回滚、无法 clone 接手；改为每次重大更新前把整个工作区复制一份带日期后缀的备份（如 `求职工作区-备份-YYYY-MM-DD/`），并在 `AGENTS.md` 记录"未启用 git"。任何汇报中不得声称"已提交"。

## 第 0 步：判断工作区状态（每次调用先做）

在当前目录（或用户指定的目录）检查初始化标记：**存在 `个人档案.md`** 即视为已初始化（是否为 git 仓库不作为判据），按用户意图路由到流程 A/B/C/D。

- 标记不存在 → 走流程 0（首次初始化）。
- 目录里已有其他项目文件 → 不要直接在原地初始化，先问用户是否新建子目录（建议名 `求职工作区/`）。
- 用户只是问"怎么写简历"这类一次性问题、没想建工作区 → 直接按流程 D 的原则口头帮助，不强制初始化。

## 流程 0：首次初始化（计划阶段）

目标：收集足够信息 → 给出可行性高的求职方向 → 用户确认 → 一次性搭好工作区。**用户确认方案前不写任何文件。**

1. **进入计划阶段**：宿主提供计划模式 / 结构化提问工具时可以借助；没有就直接用自然语言分批收集，最后以文字方案请用户明确确认。不调用、不点名任何平台专有工具。
2. **读 `references/init-guide.md`**，按其中的清单分 4 批收集信息（基本情况与学历 → 技能盘点 → 实习/项目经历深挖 → 意向与约束）。不要一次抛出所有问题；用户自由陈述时先听完再补问缺口。
3. **方向分析**（框架见 init-guide.md）：输出三层规划——
   - **保底线**：可行性最高（学历门槛够得着 + 技能匹配 + 有项目支撑），大概率拿 offer；
   - **主攻线**：能力匹配且竞争可接受，秋招主战场；
   - **冲刺线**：当前有差距，补 1–2 个短板后有机会。
   每层给出理由、对应简历版本规划、未来 2–4 周行动建议。有联网能力时先搜索当年该方向招聘行情佐证，不要凭旧印象下结论；没有联网能力就标注未做行情核验。
4. **用户确认**方向与工作区路径后，执行初始化：
   1. 确定工作区目录（默认建议当前目录下新建 `求职工作区/`，或用户指定的空目录）；
   2. 从 `assets/` 模板生成核心文件，**把收集到的信息填进去，不是盲拷贝**：AGENTS.md（概况/三线方向/文件地图，并写明是否启用 git）、个人档案.md（口径/技能表/经历 STAR）、投递跟踪.md（目标公司三线清单）、进度与薄弱点.md（按求职方向定制学习模块，不照抄模板骨架）、简历/`{{姓名}}-{{岗位}}-{{年份}}.md` 与 `.html`（出 V1 初稿或骨架）；
   3. 复制 `assets/gitignore.tpl` 为 `.gitignore`；
   4. 版本化：先 `git --version` 探测——可用则 `git init` + 首次提交：`初始化：求职工作区建立（保底{{X}}/主攻{{Y}}/冲刺{{Z}}）`；不可用则按上文「git 缺失处理」征求安装意愿，用户拒绝或安装失败时跳过 git，做一份初始手动备份，并在 AGENTS.md 标注未启用版本化；
   5. 若启用了 git：提醒用户简历含个人隐私，将来若配置远端仓库**必须私有**，并问是否现在配置远端。
5. 结束时给 1–3 条下一步建议（从进度与薄弱点.md 的任务清单挑最紧急的）。

## 流程 A：日常打卡（用户报进度）

1. 并行读取 `个人档案.md`、`投递跟踪.md`、`进度与薄弱点.md`。
2. 按用户输入对号入座回填：
   - 投递 / 笔试 / 面试结果 → `投递跟踪.md` 投递记录表；被问倒的点 → `个人档案.md §3 待补强`；
   - 刷题 / 八股 / 技能学习进展 → `进度与薄弱点.md` 对应模块（勾选 checkbox、记卡点）；
   - 实习 / 项目实质进展 → `个人档案.md §2`；**若影响简历内容** → 转流程 D 更新简历，再更新 `投递跟踪.md` 版本记录。
3. 留痕：git 可用时执行 `git add -A && git commit -m "打卡：{{日期}} {{一句话摘要}}"`；未启用 git 时做一次带日期的工作区备份，并在回复中如实说明"未启用 git，已做手动备份"，不得声称已提交。
4. 给 1–3 条下一步建议。

## 流程 B：汇报现状（用户问"我现在的进度"）

1. 并行读 `投递跟踪.md`、`进度与薄弱点.md`；git 可用时加跑 `git log --oneline -5`，不可用时改用文件修改时间说明最近更新。
2. 用一段话汇报：投递概况（各阶段数量）、简历版本状态、任务清单最紧急的事、最近一次更新时间。
3. 问用户今天想推进哪件事，然后转流程 A。

## 流程 C：面试/笔试复盘

1. 收集：公司 / 岗位 / 环节（一面二面笔试）/ 形式 / 被问问题清单 / 哪些没答好 / 自己的亮点。
2. 回填三处：失分点与原题 → `个人档案.md §3 待补强`；一行索引（日期｜公司｜环节｜最大失分点）→ `进度与薄弱点.md` 复盘索引；结果与下一步 → `投递跟踪.md` 投递记录。
3. **薄弱点归纳**：同一类问题被问倒 ≥2 次，升级为专项补强任务写入 `进度与薄弱点.md` 任务清单（标注"来源：面试复盘"）。
4. 留痕：git 可用时 `git commit -m "复盘：{{日期}} {{公司}}{{环节}}"`；未启用 git 时按流程 A 第 3 步做手动备份并如实说明。
5. 给针对性补强建议（具体到资料、练习方式、预计时长），不写空话。

## 流程 D：简历构建/更新

先读 `references/resume-guide.md`（排版要点、各平台无头打印命令、单页校验方法），然后：

1. **数据红线检查**：简历上每个数字、头衔、成果表述必须能在 `个人档案.md` 找到依据；找不到依据的先问用户，确认不了就删。这一步不可跳过——简历造假在背调里是硬伤。
2. 同步修改 `简历/*.md`（内容源）与 `简历/*.html`（排版源），两份内容必须一致。
3. 生成 PDF：按 resume-guide.md 选择当前系统可用的方式（Windows PowerShell / Git Bash、macOS、Linux 的无头打印命令；命令行与浏览器都不可用时，引导用户打开 html 用"打印 → 另存为 PDF"兜底）。
4. 单页校验：PDF 必须为 1 页。命令校验按 resume-guide.md 中当前平台对应的写法；命令结果不可靠（0、报错）时直接打开 PDF 肉眼确认页数。溢出时先压缩措辞，再动字号行距（排版参数是调过的，乱动容易整体崩）。
5. 更新 `投递跟踪.md` 的简历版本记录；git 可用时 `git commit -m "简历：{{版本}} {{改了什么}}"`，未启用 git 时做手动备份并如实说明。

## 硬规矩（违反即事故）

1. **不伪造、不夸大**。`个人档案.md §0` 的数据红线是所有简历和面试口径的唯一依据，任何场合不得复活被删掉的无依据数据。
2. **公司内部文件绝不入工作区仓库**（内部文档、代码、截图）。简历含个人隐私，启用 git 远端时仓库必须私有。
3. **每次实质修改必须留痕**：git 可用时必须提交，提交信息写清楚改了什么；未启用 git 时每次重大更新前做带日期的手动备份。信息更新靠版本历史追溯，不留痕等于丢数据；禁止在未提交也未备份时谎称已保存到历史。
4. 汇报要具体（"9 月投了 6 家，2 笔试 1 面试"），不写空话。
