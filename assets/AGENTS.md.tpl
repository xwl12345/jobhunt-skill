# AGENTS.md — {{用户名}} 求职工作区

任何 AI 助手在本目录开启会话时，先读完本文件再开始工作。本文件是接手指南：这是什么、进度在哪查、规矩是什么、活怎么干。
最后更新：{{初始化日期}}（工作区建立）。

## 一、这是什么

{{用户名}}的{{届别}}求职长期执行工作区（方向规划 / 投递 / 学习进度 / 面试复盘 / 简历），由 AI 助手长期维护。

- **用户**：{{姓名}}，{{学校}}{{专业}}{{学历层次}}，{{届别}}（{{入学年}}.09–{{毕业年}}.06）。{{一句话补充：实习/备选路径等，没有就删这行}}
- **方向三线**（初始化时规划，重大变化时更新本节）：
  - **保底线**：{{方向}} —— {{一句话理由}}
  - **主攻线**：{{方向}} —— {{一句话理由}}
  - **冲刺线**：{{方向}}（需先补：{{短板}}）
- **版本化状态**：{{已启用 git（每次实质修改后提交）/ 未启用 git——用户选择不安装，靠带日期的文件夹备份，每次重大更新前备份一次}}。
- 远端仓库（若配置）：{{远端地址}}。**必须保持私有**（简历含手机号等隐私），永远不要改为公开。

## 二、进度在哪查（唯一事实来源，先读再动手）

| 文件 | 内容 |
|---|---|
| `投递跟踪.md` | 简历版本记录 + 投递状态表 + 目标公司清单 |
| `进度与薄弱点.md` | 各模块学习进度 + 当前任务清单 + 面试复盘索引 |
| `个人档案.md` | §0 统一口径与数据红线 + 全部经历 STAR 素材 + 技能真实水平 + §3 待补强 |

**"现在的进度是什么"永远以这三个文件为准**，本文件不维护实时进度。启用 git 时首次接手再补一句 `git log --oneline -5` 看最近动过什么；未启用则看文件修改时间。

## 三、硬规矩（违反即事故）

1. **不伪造、不夸大**。数据红线清单在 `个人档案.md §0`。真实数据可写（如真实的 commit 数、测试数），无依据的不能写；被删掉的无依据数据任何场合不得复活。
2. 面试口径必须与 `个人档案.md §0` 统一口径一致（每个项目写清真实来源与角色）。
3. **公司内部文件绝不入仓库**：实习/工作中的内部文档、代码、截图保持在工作区之外。
4. 每次实质修改必须留痕：启用 git 时 `git add -A && git commit -m "..."`（Windows PowerShell 5.1 不支持 `&&`，拆成两条依次执行），提交信息写清楚改了什么；未启用 git 时做一次带日期的工作区备份，不得在未留痕时声称已保存到历史。
5. **简历双源同步**：改简历必须同步改 `.md`（内容源）→ `.html`（排版源），重新生成 PDF 并确认单页，再更新 `投递跟踪.md` 的版本记录。

## 四、简历文件

| 版本 | 定位 | 文件（简历/ 目录） |
|---|---|---|
| {{V1}} | {{保底方向}} | `{{姓名}}-{{岗位}}-{{年份}}校招.md/.html/.pdf` |
| {{V2}} | {{主攻方向}} | （按需增加） |

生成 PDF：用 Edge/Chrome/Chromium 任一无头打印（Windows PowerShell/Git Bash、macOS、Linux 的命令与单页校验方法见 AI 助手的 jobhunt skill 的 resume 指引；本仓库零依赖，不需要 Python / Word；命令行不可用时浏览器"打印为 PDF"兜底）。

## 五、会话节奏

- 用户报进度（投递 / 刷题 / 面试 / 学习进展 / 简历改动）→ 回填对应进度文件 → 留痕（git 提交，或未启用 git 时做带日期备份）。
- 里程碑事件（简历改版、投递一批、面试复盘、offer）必须留痕一次。
- 换电脑 / 换 AI 助手接手：启用 git 且配了远端时 `git clone {{远端地址}}`，clone 后立刻设仓库级 git 身份：`git config user.name {{git用户名}} && git config user.email {{git邮箱}}`（免疫机器全局配置残留）；未启用 git 时直接拷贝整个工作区目录亦可。任何 AI 助手在本目录开会话时，先读完本 AGENTS.md 即可接手（AGENTS.md 是开放的 agent 接手约定，不绑定特定工具）。
